/** Keep in sync with PUBLIC_POLITICI_URL and src/proxy.ts. */
export const POLITICI_HOST = "politici.dovevannoinostrisoldi.com";
/** Set by the proxy so the root layout can strip chrome before hydration. */
export const IMMERSIVE_REQUEST_HEADER = "x-dvns-immersive";
export const IMMERSIVE_REQUEST_VALUE = "politici";
